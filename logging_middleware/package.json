{
  "name": "logging-middleware",
  "version": "1.0.0",
  "description": "Reusable logging middleware that sends structured logs to a remote log collection service",
  "main": "index.js",
  "scripts": {
    "test": "node test.js"
  },
  "keywords": ["logging", "middleware", "observability"],
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.7.0"
  }
}
